// Question - check for subtree in binary tree

// we have two trees and we have to find if second tree is in the first tree
// we will do a level order and use areIdentical function whenever we find the same root;